const buttons=[...document.querySelectorAll('.status-choice')], status=document.getElementById('status');function select(v){status.value=v;buttons.forEach(b=>b.classList.toggle('selected',b.dataset.status===v));}buttons.forEach(b=>b.onclick=()=>select(b.dataset.status));async function load(){const r=await fetch('/api/state');const s=await r.json();select(s.status);messageInput.value=s.message||'';returnInput.value=s.return_time||'';}statusForm.onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(statusForm));const r=await fetch('/api/state',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(r.ok)location.href='/';else alert('Could not update sign.');};load();
